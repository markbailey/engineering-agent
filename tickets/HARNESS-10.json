{
  "ticket_id": "HARNESS-10",
  "title": "Rate Limit Awareness",
  "type": "Story",
  "requirements": "Respect API rate limits. Add jitter to prevent thundering herd.\n\n1. Create scripts/rate-limit.sh:\n   - Parse GitHub API rate limit headers from gh api output: X-RateLimit-Remaining, X-RateLimit-Reset, Retry-After\n   - If remaining < 10: preemptive backoff until reset time\n   - Log rate limit state to run.log\n\n2. Add jitter to retry-with-backoff.sh:\n   - Add jitter: delay * (1 + random(0, 0.3))\n   - Jitter via: $((delay + RANDOM % (delay * 30 / 100)))\n   - Prevents synchronized retries from parallel runs\n\n3. Integrate rate limit awareness:\n   - Update scripts that call gh: check-team-conflict.sh, worktree-cleanup.sh, check-pr-size.sh\n   - Before gh calls: check rate limit state, preemptive wait if near limit\n   - After gh calls: parse response headers, update rate limit state\n   - Add rate limit event to dashboard (new event type in notify.sh)\n\n4. Write tests:\n   - Test jitter produces different delays across runs (expect non-deterministic)\n   - Test preemptive backoff when remaining=5 (expect wait logged)\n   - Test retry-with-backoff.sh with jitter enabled (expect increasing but varied delays)",
  "acceptance_criteria": [
    "scripts/rate-limit.sh exists and parses GitHub API rate limit headers",
    "Preemptive backoff triggers when remaining < 10",
    "Rate limit state logged to run.log",
    "retry-with-backoff.sh includes jitter in delay calculation",
    "Jitter produces non-deterministic delays",
    "Scripts calling gh (check-team-conflict.sh, worktree-cleanup.sh, check-pr-size.sh) check rate limits",
    "notify.sh supports rate_limit event type for dashboard",
    "All tests pass"
  ],
  "repo": "HARNESS",
  "linked_issues": ["HARNESS-9"]
}
